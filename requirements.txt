# --- CORE API (FASTAPI) ---
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0
jinja2==3.1.3
python-multipart==0.0.9      # INDISPENSABLE pour l'upload de fichiers
aiofiles==23.2.1
requests==2.31.0
pydantic==2.6.1
email-validator==2.1.0.post1 # INDISPENSABLE pour la validation Auth
firebase-admin

# --- DATA SCIENCE & MATHS (CORTEX ENGINE) ---
pandas==2.2.0
numpy==1.26.3
openpyxl==3.1.2              # Moteur Excel (SGE/GRDF)
xlrd>=2.0.1 
pyarrow==15.0.0              # Accélérateur Pandas
chardet==5.2.0               # Détection encodage CSV

# --- FINANCE & PDF (CORTEX FINANCE) ---
pdfplumber==0.10.3           # Extraction PDF Factures
workalendar==17.0.0          # Calendrier France (Jours fériés)
holidays==0.41

# --- CLOUD & AI ---
google-cloud-aiplatform>=1.38.0

# --- SÉCURITÉ & AUTH (ROBUSTE - SANS CROCHETS) ---
passlib==1.7.4
bcrypt==4.1.2                # Moteur de hachage explicite (évite les erreurs)
python-jose==3.3.0
cryptography==42.0.2         # Moteur crypto explicite (évite les erreurs)
pyotp==2.9.0                 # Double Auth (Google Authenticator)
qrcode==7.4.2                # Génération QR Code

