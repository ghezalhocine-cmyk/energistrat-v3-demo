# --- CORE API (FASTAPI) ---
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0
jinja2==3.1.3
python-multipart==0.0.9      # MISE A JOUR : 0.0.9 recommandé pour la stabilité UploadFile
aiofiles==23.2.1
requests==2.31.0
pydantic==2.6.1
email-validator==2.1.0.post1 # AJOUT CRITIQUE : Indispensable pour valider les emails (Auth)

# --- DATA SCIENCE & MATHS (CORTEX ENGINE) ---
pandas==2.2.0
numpy==1.26.3
openpyxl==3.1.2
xlrd>=2.0.1 
pyarrow==15.0.0
chardet==5.2.0

# --- FINANCE & PDF (CORTEX FINANCE) ---
pdfplumber==0.10.3           # MISE A JOUR : Version 0.10.3 pour meilleure extraction tableaux
workalendar==17.0.0          # CRITIQUE : Pour la projection intelligente (Jours fériés France)
holidays==0.41

# --- CLOUD & AI ---
google-cloud-aiplatform>=1.38.0

# --- SÉCURITÉ & AUTH (CORTEX AUTH) ---
passlib[bcrypt]==1.7.4       # Hachage des mots de passe
python-jose[cryptography]==3.3.0 # Génération des Tokens JWT
pyotp==2.9.0                 # Moteur Double Authentification (TOTP)
qrcode==7.4.2                # Génération du QR Code pour Google Auth

